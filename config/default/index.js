/**
 * @description 导出默认配置(通用配置|主题配置|网络配置)
 **/
import setting from './setting.config'
import theme from './theme.config'
import network from './net.config'
import msgConfig from './msg.config'
export  { setting, theme, network, msgConfig }
